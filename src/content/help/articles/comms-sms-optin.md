# SMS opt-in rules

We never send SMS to anyone who hasn't explicitly opted in. The flag is `sms_opt_in` on each attendee / profile, set at one of:
- Self-service via the public `/welcome` form
- Staff-entered (with verbal/written consent recorded)
- Profile setup

See the full policy at `/sms-policy`. Reply **STOP** to any message to opt out.
