# Sending emails & SMS

Open **Admin → Communications** (admins / team leads). You can:
- Compose ad-hoc emails or SMS with the AI-assisted composer
- Use saved templates with `{{placeholders}}` like `{{first_name}}`
- Filter recipients by tag, team, role, or pipeline stage

All sends are logged to `email_log` / `sms_log` for audit.
