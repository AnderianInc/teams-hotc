# Pick ONE transport:

# Network printer (preferred for QL-1110NWB on wifi)
PRINTER_HOST=192.168.1.60
PRINTER_PORT=9100

# Linux USB
# USB_DEVICE=/dev/usb/lp0

# Windows (printer must be installed in Windows under this exact name)
# WIN_PRINTER=Brother QL-1110NWB

HTTP_PORT=9999
HTTPS_PORT=9443
